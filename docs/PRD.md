# 产品需求文档(PRD):持仓股管理与交易复盘工具

> 状态:迭代中 · 最后更新:2026-05-30 · 负责人:AllenMakerZ

---

## 1. 背景与问题

- **要解决的问题:**  
  A 股个人投资者在做交易复盘时，面临三个割裂问题：
  1. **成交数据分散**——只有券商 App 里的原始流水，没有统一的持仓成本 / 盈亏汇总视图；
  2. **交易思路无法追溯**——下单时的判断依据（逻辑、信心度、标签）没有地方记录，事后无从复盘；
  3. **手工整理效率低**——从同花顺导出 .xls 后，还要手动在 Excel 里做 FIFO 成本配对，费时且易出错。

- **为什么现在做:**  
  持仓规模已形成、成交流水样本已具备（`20260421_20260528_Atrading.xls`），是从"凭感觉"升级到"数据驱动复盘"的合适时机。若继续推迟，历史交易意图将无法补录，复盘价值折损。

---

## 2. 目标与成功指标

- **目标:**  
  建立一个以"真实成交 + 交易意图 + 大模型复盘"为核心的个人 A 股复盘闭环；本期先把数据底座跑通，后期接入 AI 分析。

- **成功指标:**

  | 指标 | 目标值 |
  |------|--------|
  | 导入同花顺 .xls 文件并完成入库 | 成功率 ≥ 99%（非格式异常行） |
  | FIFO 盈亏计算与手工对账误差 | 金额差 = 0（Decimal 精度） |
  | 添加一笔交易意图标签耗时 | < 30 秒 |
  | 同一文件重复导入 | 系统自动拦截，不产生重复数据 |
  | 页面数据刷新（含行情） | < 3 秒内展示结果 |

---

## 3. 目标用户与场景

- **主要用户 / 角色:**  
  个人 A 股投资者（单用户工具，无多人协作需求）。

- **核心使用场景:**

  | 场景 | 触发时机 | 期望操作路径 |
  |------|----------|-------------|
  | **查看整体账户状态** | 每日开盘前/收盘后 | 打开总览页 → 一眼看到净值曲线、绩效摘要、当日大盘情绪 |
  | **导入成交** | 定期从同花顺导出流水 | 总览页拖拽上传 .xls → 解析完成 → 当页显示结果摘要 |
  | **查看持仓** | 任意时刻想了解当前持仓 | 打开总览页 → 滚动到底部持仓区块 → 看到各票持仓数量、均价、当前市值、浮动盈亏 |
  | **查看盈亏** | 阶段性复盘 | 打开盈亏页 → 看到已实现 / 未实现 / 当日盈亏汇总及明细 |
  | **记录交易意图** | 买卖后即时记录或事后补录 | 找到对应成交 → 填写思路文本、选标签、打信心度 |
  | **（未来）触发 AI 复盘** | 想对某笔或某票做深度复盘 | 选定范围 → 大模型生成分析报告（功能 4，本期预留） |

---

## 4. 功能需求

### 4.0 交易总览页面（Dashboard）

> 系统默认首页，将「账户表现 + 成交流水」集成于一屏；文件导入入口位于顶部导航栏。文件解析逻辑由 4.1 提供，本节只定义 UI 呈现与交互。大盘情绪与持仓明细已移至「当日持仓」页面（见 4.6）。

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 0.1 | 账户净值曲线 | 作为投资者，我想看到账户净值随时间的变化曲线，以便直观感知资金增长与回撤历史 | P0 | 折线图展示账户总资产（持仓市值合计）按交易日变化；以最早入库成交日为起点，净值归一化为1.0 |
| 0.2 | 净值 vs 大盘对比 | 作为投资者，我想在同一图表叠加沪深300走势，以便判断自己的相对/绝对收益水平 | P0 | 同图叠加沪深300指数同期涨跌幅，两线同起点归一化；点击可切换上证综指；数据来源：akshare（免费，无积分门槛）|
| 0.3 | 绩效摘要卡片 | 作为投资者，我想在页面顶部一眼看到核心绩效数字 | P0 | 展示四项指标：总收益率、最大回撤、累计已实现盈亏、当前浮动盈亏；数值实时计算 |
| 0.4 | 成交流水列表 | 作为投资者，我想在总览页底部直接看到所有历史成交记录，以便不必跳转独立页面追溯成交细节 | P0 | 总览页底部展示成交流水表格；支持按股票代码/名称、买卖方向筛选；每行含日期、股票、方向、成交价、数量、成交金额、意图标签；提供「编辑意图」与「AI 复盘」操作入口 |
| 0.5 | 文件上传入口 | 作为投资者，我想通过顶部导航栏直接上传同花顺文件，以便随时导入不必进入专门页面 | P0 | 顶部第一行含「上传文件」按钮；上传后调用 4.1 解析逻辑；完成后弹窗展示摘要：成功入库行数、跳过重复行数、失败行数（含原因） |
| 0.6 | 上传历史记录 | 作为投资者，我想通过顶部导航栏查看历次导入批次，以便确认哪些文件已导入 | P1 | 顶部第一行含「导入历史」按钮，点击弹窗展示 `import_batches` 列表：文件名、导入时间、覆盖日期区间、成功入库行数 |

---

### 4.1 交易导入模块（功能 2）

> UI 入口在总览页（4.0 需求 0.5/0.6），本节定义后端解析与入库逻辑。

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 2.1 | 上传同花顺导出文件 | 作为投资者，我想上传 .xls/.csv 文件，以便把成交数据自动入库，不再手工录入 | P0 | 上传 `20260421_20260528_Atrading.xls`（GB18030 + Tab 分隔）后，所有可解析行进入 `trades` 表；字段齐全 |
| 2.2 | 担保品划入单独识别 | 作为投资者，我想让系统区分"担保品划入"与"买入"，以便成本计算不被虚增 | P0 | `买卖标志=担保品划入` 的行映射为 `side=transfer_in`，不计入买入资金投入统计 |
| 2.3 | 重复导入拦截 | 作为投资者，我想防止同一文件重复导入，以便数据库不出现重复流水 | P0 | 同一 `file_hash` 二次上传时，系统提示"已导入"并拒绝入库 |
| 2.4 | 逐行去重 | 作为投资者，我想让系统自动忽略重复成交行，以便多次上传时不产生重复记录 | P0 | `(trade_date, stock_code, side, price, quantity, amount)` 唯一约束；重复行跳过并在结果中标注 |
| 2.5 | 错误行提示 | 作为投资者，我想知道哪些行解析失败及原因，以便我能手动处理异常数据 | P0 | 返回失败行列表（行号 + 原始文本 + 错误原因）；失败行不阻断其余行入库 |
| 2.6 | 原始行留存 | 作为投资者，我想保留原始导入文本，以便日后重新解析或追溯 | P1 | 每行原始文本存入 `raw_import_rows`，可通过 UI 或 API 查询 |
| 2.7 | HTML 伪装 .xls 兼容 | 作为投资者，我想导入其他券商的 .xls，以便工具不只支持同花顺 | P2 | 检测到 `<table>` / `<html>` 标记时自动切换 `pandas.read_html()` 解析 |
| 2.8 | 手续费自动计算 | 作为投资者，我想让系统按规则自动算出每笔手续费，以便盈亏扣费后的净收益更准确 | P1 | 按以下规则计算 `fee` 字段并落库：① **佣金** = max(成交金额 × 0.00008, 5.00)，双向；② **印花税** = 成交金额 × 0.0005，仅卖出方向；③ **过户费**（沪A）= 成交金额 × 0.00001，双向；深A 过户费已含于经手费，不单独计算；`transfer_in` 行不计费 |

---

### 4.2 持仓与盈亏分析模块（功能 1）

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 1.1 | 实时持仓总览 | 作为投资者，我想看到当前所有持仓股的数量、成本均价、最新价、市值，以便掌握资产全貌 | P0 | 「当日持仓」页面持仓明细区块展示：股票代码、名称、持仓数量、均价、最新价（行情服务）、市值、浮动盈亏金额 & 百分比；保留单票 AI 复盘入口 |
| 1.2 | FIFO 已实现盈亏 | 作为投资者，我想按先进先出算出每笔卖出的已实现盈亏（含手续费），以便准确评估历史净收益 | P0 | 卖出时与最早买入批次配对；净盈亏 = 卖出收入 − 买入成本 − 买入费用 − 卖出费用；计算结果与手工 FIFO 核对金额差 = 0；注：数值与券商 App（加权平均）展示会有正常差异 |
| 1.3 | 未实现盈亏（浮动） | 作为投资者，我想看到当前持仓的浮动盈亏，以便判断是否持续持有 | P0 | 浮动盈亏 = 持仓数量 × (最新价 − 持仓均价)；最新价来自行情服务 |
| 1.4 | 当日盈亏 | 作为投资者，我想看到今日持仓的涨跌盈亏，以便做日内复盘 | P1 | 当日盈亏 = 持仓数量 × (最新价 − 昨收价) |
| 1.5 | 交易流水列表 | 作为投资者，我想浏览所有历史成交记录，以便追溯任意成交细节 | P0 | 交易总览页底部成交流水区块，支持按股票代码/名称、买卖方向筛选；展示所有 `trades` 表字段；不再提供独立流水页 |
| 1.6 | 行情服务（TuShare 主 + akshare 兜底） | 作为投资者，我想让系统自动获取最新行情，以便无需手动填价格 | P0 | TuShare 失败时自动 fallback 到 akshare；上层无感知；两者均失败时页面提示"行情获取失败"而非崩溃 |

---

### 4.3 交易意图模块（功能 3）

> 本模块分四个子能力：**意图录入**（打标签 + 记思路）→ **意图详情视图**（关联流水 + P&L）→ **标签管理** → **复盘统计看板**。

#### 意图录入与关联

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 3.1 | 对单笔成交打标签 | 作为投资者，我想对某笔成交打上分类标签（如"突破""补仓""止损"），以便按标签维度复盘交易系统执行情况 | P0 | 从流水页点击任意成交 → 弹出编辑面板 → 多选标签 → 保存成功；标签以徽标形式显示在该行成交记录上 |
| 3.2 | 记录交易思路与信心度 | 作为投资者，我想在打标签的同时写下当时的思路和信心度，以便复盘时还原决策背景 | P0 | 同一编辑面板内可填 `thesis` 自由文本 + `confidence`（1-5 单选）；均为可选填，不强制 |
| 3.3 | 对股票整体记录意图 | 作为投资者，我想记录对某只股票的整体判断，不必绑定某笔具体成交 | P1 | 意图可只填 `stock_code`，不关联 `trade_id`，仍可正常保存和查询 |

#### 意图详情视图

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 3.4 | 意图-流水关联视图 | 作为投资者，我想在意图详情页看到该意图关联的所有成交流水，以便全貌了解一次交易的过程 | P0 | 详情页展示关联的 `trades` 列表，列含：成交日期、买卖方向、成交价、数量、成交金额、手续费；按时间顺序排列 |
| 3.5 | 关联 P&L 展示 | 作为投资者，我想在意图详情页直接看到这笔交易的盈亏，以便与当初意图对照评估决策质量 | P0 | 详情页展示：已实现盈亏（若已全部卖出）或浮动盈亏（若仍持仓）；金额与盈亏率均显示；费用已扣除 |
| 3.6 | 意图列表与检索 | 作为投资者，我想按股票、标签、时间范围筛选历史意图，以便快速找到特定主题的复盘记录 | P1 | 意图列表支持按 `stock_code`、`tags`、日期区间筛选；每条显示股票代码、标签、思路前80字、盈亏摘要；按 `created_at` 倒序 |

#### 标签管理

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 3.7 | 自定义标签 | 作为投资者，我想新增/删除/重命名自己的交易标签，以便标签体系匹配自己的交易系统分类 | P1 | 标签管理页可增删标签；删除时提示该标签已关联的意图数；MVP 以 JSON 存储，无需单独关联表 |

#### 复盘统计看板

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 3.8 | 交易胜率与盈亏比 | 作为投资者，我想看到整体及分标签的胜率和盈亏比，以便评估交易系统的长期期望值 | P0 | 展示：总胜率（已盈利的已平仓round-trip / 总已平仓round-trip）；平均盈利幅度 / 平均亏损幅度；支持按标签维度拆分查看 |
| 3.9 | 交易系统符合率 | 作为投资者，我想看到有多少笔交易打了标签，以便量化自己的交易纪律执行情况 | P1 | 展示：已打标签的成交笔数 / 总成交笔数（代理"按系统执行"的比率）；支持按时间段筛选；低于60%时给出"纪律执行偏低"提示 |
| 3.10 | 仓位换手率与心态趋势 | 作为投资者，我想看到近期仓位换手率的变化趋势，以便判断自己最近是否处于频繁操作或情绪化状态 | P1 | 展示：每自然月的换手率（月内总成交金额 / 月初总持仓市值）；近6个月折线趋势图；单月换手率超过200%时标注"⚠ 操作频繁" |
| 3.11 | 标签绩效分析 | 作为投资者，我想按标签维度查看每类交易策略的胜率和平均盈亏，以便优化交易系统各模块 | P2 | 按标签分组展示：交易次数、胜率、平均盈亏金额、平均持仓天数；支持排序 |

---

### 4.4 大模型辅助复盘（功能 4）

> 使用本地 **Ollama**，无需外部 API Key。用户需自行安装 Ollama 并拉取所需模型（推荐 `qwen2.5:14b` 或 `deepseek-r1:7b`）。

#### Ollama 配置

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 4.1 | Ollama 连接配置 | 作为投资者，我想在设置页填写 Ollama 地址和模型名称，以便切换本地不同模型 | P0 | 设置页可配置：Ollama Base URL（默认 `http://localhost:11434`）、模型名称（默认 `qwen2.5:14b`）；保存后系统 ping Ollama 验证连通性，成功/失败有明确提示 |

#### 复盘触发与报告生成

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 4.3 | 单笔成交复盘 | 作为投资者，我想对某笔具体成交触发 AI 复盘，以便深入分析单次决策的得失 | P0 | 从流水页或意图详情页点击"AI 复盘"→ 后端组装该笔成交的：成交数据 + 当日及前后行情 + 关联意图（标签/思路/信心度）+ **交易系统规则（来自 4.5 模块）** → 调 Ollama → 流式返回报告 → 展示在复盘详情页 |
| 4.4 | 单只股票复盘 | 作为投资者，我想对某只股票的全部成交历史做整体 AI 复盘，以便评估整体操作质量 | P0 | 从总览页持仓区块或流水页选定股票触发 → 组装该股所有成交 + 行情区间 + 所有关联意图 → 调 Ollama → 流式返回报告 |
| 4.5 | 时间段复盘 | 作为投资者，我想对指定时间段内的所有交易做整体 AI 复盘，以便做阶段性总结 | P1 | 复盘页可选起止日期 → 组装该时段所有成交 + 意图 + 规则 → 调 Ollama → 流式返回报告 |
| 4.6 | 复盘报告持久化 | 作为投资者，我想保存每次 AI 复盘的报告，以便日后回顾和对比 | P0 | 每次复盘完成后自动存入 `review_reports`，含 `input_snapshot`（喂给模型的结构化数据快照，用于复现）；报告列表页按时间倒序展示 |
| 4.7 | 历史报告查看 | 作为投资者，我想随时重新查看历史复盘报告，以便追踪判断的变化 | P1 | 复盘报告列表页展示：复盘范围、触发时间、模型名称；点击展开报告全文 |

---

### 4.6 当日持仓页面（/holdings）

> 顶层导航独立页，聚焦「今日视角」：先展示当天大盘情绪（盘面解析），再展示持仓明细，最后提供明日操作思路记录区。大盘情绪与持仓明细均从此处统一查看，不再分散在其他页面。

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 6.1 | 盘面解析区块 | 作为投资者，我想在持仓页顶部看到当日大盘情绪，以便在查看持仓时快速感知市场背景 | P0 | 三栏展示：① 市场情绪（上涨/下跌/平盘家数 + 偏多/中性/偏空标签）；② 涨跌停统计（涨停家数、跌停家数、涨跌停比）；③ 资金概况（全市场成交额、数据日期、交易状态）；数据来源 akshare；非交易日提示休市 |
| 6.2 | 持仓明细区块 | 作为投资者，我想紧接大盘情绪之后看到当前持仓，以便在同一页完成「市场背景 → 我的持仓」的完整扫视 | P0 | 展示总持仓市值、总浮动盈亏、持仓股票数汇总行；持仓表格字段：股票、持仓数量、持仓均价、最新价、市值、浮动盈亏、盈亏率；表头可排序；每行保留「AI 复盘」入口 |
| 6.3 | 明日操作思路 | 作为投资者，我想在收盘后在持仓页记录明日的操作计划，以便形成每日复盘闭环 | P1 | 页面底部提供自由文本输入框（多行）；点击「保存」本地持久化（localStorage，后续同步服务端）；保存后按钮短暂变为「已保存」反馈；内容在页面重载后保留 |

---

### 4.5 交易系统规则模块（独立页面）

> **顶层导航独立页**，与总览、当日持仓、复盘并列，可随时直接切换查看。  
> 用于沉淀和维护个人交易系统的纪律文本，并作为大模型复盘的核心上下文。

| 编号 | 子功能 | 用户故事 | 优先级 | 验收标准 |
|------|--------|----------|--------|----------|
| 5.1 | 规则内容编辑 | 作为投资者，我想在专属页面写下并维护自己的交易系统规则，以便随时查阅和迭代 | P0 | 页面提供富文本/Markdown 编辑区，支持分章节编写（如"仓位管理""买入信号""止损纪律"）；保存后持久化；内容长度不限 |
| 5.2 | 规则固定展示 | 作为投资者，我想在页面左侧/顶部固定看到规则大纲，以便快速定位具体章节 | P1 | 侧边栏或顶部 Tab 展示规则章节目录；点击章节跳转到对应内容区 |
| 5.3 | 规则版本历史 | 作为投资者，我想看到交易系统规则的修改历史，以便理解自己的交易理念是如何演变的 | P2 | 每次保存自动生成版本快照（时间戳 + 变更摘要）；可查看并还原历史版本 |
| 5.4 | 规则-复盘联动 | 作为投资者，我想在复盘分析页直接引用当前规则，以便 AI 能依据最新版本评估交易纪律 | P0 | 触发复盘时，系统自动取 `rules` 表中最新版本的规则全文注入 prompt；复盘报告的 `input_snapshot` 中记录所用规则版本ID，保证可复现 |

---

## 5. 范围与非目标

**本期范围（In scope）:**
- **交易总览页面**（账户净值曲线 vs 大盘对比、绩效摘要四卡片、成交流水列表、文件上传入口与历史）
- **当日持仓页面**（盘面解析三栏、持仓明细、明日操作思路文本框）
- 功能 2 交易导入（同花顺 .xls 解析、入库、去重、错误处理）
- 功能 1 盈亏分析（FIFO 计算、持仓总览、行情接入；流水列表并入交易总览）
- 功能 3 交易意图（意图录入、关联流水+P&L 视图、标签管理、复盘统计看板）
- 功能 4 大模型复盘（本地 Ollama、单笔/单票/时间段三种触发、报告持久化）
- **交易系统规则**（独立顶层页面、Markdown 编辑、版本历史、与复盘联动）

**明确不做（Non-goals）:**
- 多用户 / 账号体系、权限管理
- 移动端（iOS / Android）原生应用
- T+0 分钟级盈亏（数据源仅到日级，导入文件无成交时间戳）
- A 股以外市场（港股、美股、期货）
- 持仓股新闻功能（整体删除）
- 推送通知（企业微信 / Server酱 / 邮件等）
- 数据导出（PDF / Excel 报告）

---

## 6. 开放问题与假设

| # | 类别 | 描述 | 结论 |
|---|------|------|------|
| Q1 | ✅ 已确认 | 持仓成本算法 | **FIFO（先进先出）**。复盘工具需追踪每批买入的具体盈亏，FIFO 能清晰还原"哪笔买入 → 哪笔卖出"的决策链路；加权平均会平滑每批决策的 P&L，不适合做复盘分析。注意：FIFO 结果与券商 App 显示的加权均价会有差异，属正常现象 |
| Q2 | ✅ 已确认 | 手续费计算 | **按规则自动计算**，不依赖交割单。佣金万0.8（最低¥5，双向）+ 印花税0.05%（卖出方向）+ 过户费0.001%（沪A双向，深A已含于经手费）。详见需求 2.8 |
| Q3 | ✅ 已确认 | 功能 4 大模型 | **本地 Ollama**，无需 API Key 与外部依赖；后续视需求再评估切换云端模型（Deepseek/Claude 等） |
| Q4 | ✅ 已确认 | 功能 5 新闻 | **整体删除**，不做新闻功能 |
| Q5 | ✅ 已确认 | 部署模式 | 单用户本地部署，数据存于本机 SQLite；无多设备同步需求 |
| Q6 | ✅ 已确认 | TuShare 积分 | A 股日线行情（`daily` 接口）最低需要 **120 积分**，可通过免费注册 + 完成账号认证步骤（手机/邮箱绑定）达到；50次/分钟的调用频次对本工具完全够用；akshare 作为兜底 |

---

## 7. 变更记录

| 日期 | 类型 | 内容 | 原因 |
|------|------|------|------|
| 2026-05-29 | 新建 | 初版，基于技术方案 v1 整理产出 | 项目启动 |
| 2026-05-29 | 变更 | 确认 Q1-Q6 全部开放问题；新增手续费自动计算需求(2.8)；功能4改用本地Ollama；功能5改为网页展示不做推送；非目标补充说明 | 用户答复 |
| 2026-05-29 | 变更 | 重写功能3交易意图模块：拆分为意图录入/详情视图/标签管理/复盘统计看板四个子模块；新增意图-流水关联视图(3.4)、关联P&L展示(3.5)、胜率盈亏比(3.8)、交易系统符合率(3.9)、换手率心态趋势(3.10)、标签绩效分析(3.11) | 用户补充需求 |
| 2026-05-29 | 新增 | 新增总览页面模块(4.0)：账户净值曲线vs大盘对比(akshare)、绩效摘要卡片、当日大盘情绪（涨跌家数/涨停数/成交额/情绪标签）、文件上传入口与历史；4.1导入模块UI入口移至总览页 | 用户补充需求 |
| 2026-05-29 | 变更 | 功能4从预留改为实际实现：Ollama配置(4.1-4.2)、单笔/单票/时间段复盘触发(4.3-4.5)、报告持久化与查看(4.6-4.7)；功能5新闻模块整体删除 | 用户决策 |
| 2026-05-29 | 新增 | 新增4.5交易系统规则独立页面模块：顶层导航入口、Markdown分章节编辑(5.1-5.2)、版本历史(5.3)、与复盘prompt联动(5.4)；从4.4设置页中移出 | 用户决策 |
| 2026-05-30 | 变更 | 删除独立持仓页入口；将实时持仓总览合并到总览页底部；单只股票复盘入口改为从总览页持仓区块或流水页触发 | 用户要求持仓页面直接删除，持仓部分放到总览页最下面 |
| 2026-05-30 | 重构 | 页面结构全面调整：① 总览页改名「交易总览」，删除大盘情绪卡片和持仓区块，底部新增成交流水（原流水页内容合并，流水独立页删除）；② 盈亏页改名「当日持仓」（路由 `/holdings`），内容全部替换为盘面解析+持仓明细+明日操作思路；③ 新增 4.6 当日持仓页面模块；④ 导航项从 6 项精简为 5 项（交易总览/当日持仓/交易意图/复盘报告/交易规则） | 用户决策 |
